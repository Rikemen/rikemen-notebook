# Project Guidelines (SSoT)

このファイルはプロジェクト固有のガイドラインを記述する場所です。
技術的な決定事項、アーキテクチャ、コマンドなどをここに追記してください。

## 参照先
- **デザイン** `DESIGN.md`を参照してください。
- **行動規範:** `.agent/rules/` を参照してください。
- **ワークフロー:** `.agent/workflows/` を参照してください。
- **スキル:** `.agent/skills/` を参照してください。

## Gitブランチの確認

- Codexはファイルの変更、生成、削除を始める前に、必ず`git branch --show-current`を実行する。
- 現在のブランチが`main`または`master`の場合は作業を中止し、ファイルを変更しない。
- `main`または`master`に未コミット変更がある場合は、変更を破棄、stash、resetせず、状態を利用者へ報告する。
- 開発は`dev`または作業内容に対応した`feat/`、`fix/`、`docs/`ブランチへ切り替えてから開始する。
