# Gauss Notebook — Design System

## 1. 目的

「Gauss Notebook」は、資料PDF、Markdownノート、AIチャット、スケッチ実行環境を一つのワークスペース上に配置し、自分専用の数学ノートを組み立てるWebアプリである。

本デザインでは、**モノクロを基調としたニューモーフィズム**を採用する。柔らかい立体感によって多数のパネルを整理しつつ、操作対象は青、警告・破壊的操作は赤で明確に区別する。

---

## 2. デザイン原則

### 2.1 静かな作業空間

- 背景、パネル、入力欄は白〜ライトグレーで統一する。
- 数式・文章・図表が主役になるよう、装飾色は最小限に抑える。
- 影は「境界線の代わり」ではなく、階層を補助するために使う。

### 2.2 触れられるUI

- ボタン、タブ、パネルは柔らかく浮き上がる形状にする。
- 押下中・選択中・入力中は、内側に沈み込む表現へ切り替える。
- ドラッグ、リサイズ、最小化、最大化など、パネル操作の手掛かりを常に表示する。

### 2.3 色の役割を固定する

- **青**：主要操作、選択状態、フォーカス、AI支援、リンク、実行。
- **赤**：エラー、削除、取消不能な操作、実行失敗、警告。
- 上記以外は原則としてモノクロで表現する。

### 2.4 ニューモーフィズムと可読性の両立

- 影だけでボタンや入力欄を識別させない。
- 1pxの薄い境界線、ラベル、アイコン、状態色を併用する。
- テキストと背景のコントラスト比は原則4.5:1以上とする。

---

## 3. カラーパレット

### 3.1 Monochrome

| Token | Hex | 用途 |
|---|---:|---|
| `--color-bg` | `#EEF1F5` | アプリ全体の背景 |
| `--color-surface` | `#F4F6F9` | パネル、カード、ボタン |
| `--color-surface-strong` | `#FFFFFF` | PDF、ホワイトボード、コード領域 |
| `--color-surface-inset` | `#E9EDF2` | 入力欄、押下中、選択された内側表現 |
| `--color-border` | `#D6DCE5` | 補助境界線 |
| `--color-text-primary` | `#171A20` | 見出し、本文、数式 |
| `--color-text-secondary` | `#5F6875` | 補足、日時、説明 |
| `--color-text-muted` | `#8C95A3` | 無効状態、プレースホルダー |
| `--color-icon` | `#343A44` | 通常アイコン |

### 3.2 Accent Blue

| Token | Hex | 用途 |
|---|---:|---|
| `--color-blue` | `#1267E8` | 主要ボタン、選択、グラフ |
| `--color-blue-hover` | `#0D56C5` | Hover |
| `--color-blue-active` | `#09459F` | Pressed |
| `--color-blue-soft` | `#E7F0FF` | 選択背景、AIメッセージ、補助表示 |
| `--color-blue-focus` | `#6EA4FF` | フォーカスリング |

### 3.3 Accent Red

| Token | Hex | 用途 |
|---|---:|---|
| `--color-red` | `#D9434E` | エラー、削除、危険操作 |
| `--color-red-hover` | `#BB313B` | Hover |
| `--color-red-active` | `#96262E` | Pressed |
| `--color-red-soft` | `#FCEAEC` | エラー背景、警告表示 |

### 3.4 色の使用ルール

- 1画面内で青い主要ボタンは、原則1つの操作グループにつき1つまで。
- 閉じるボタンは通常時モノクロ、Hover時のみ赤にする。
- 保存完了は青いチェックまたはモノクロ表示とし、緑は使用しない。
- グラフの基本系列は青、比較系列は濃淡グレー、エラーや除外領域のみ赤を使う。

---

## 4. タイポグラフィ

```css
font-family: Inter, "Noto Sans JP", "Hiragino Sans", "Yu Gothic UI", sans-serif;
```

数式表示には KaTeX または MathJax を用い、数式フォントは `STIX Two Math` 相当を推奨する。

| Style | Size / Line-height | Weight | 用途 |
|---|---|---:|---|
| App title | 24px / 32px | 700 | アプリ名 |
| Page title | 20px / 28px | 700 | ノート名 |
| Panel title | 15px / 22px | 700 | 各パネル名 |
| Section title | 13px / 20px | 700 | パネル内区分 |
| Body | 14px / 22px | 400 | 本文、チャット |
| UI label | 13px / 18px | 500 | ボタン、タブ |
| Caption | 11px / 16px | 400 | 日時、容量、補足 |
| Code | 12px / 19px | 400 | コードエディタ |

- UI本文に手書き風フォントは使わない。
- ホワイトボード上の手書き入力表現に限り、手書き風フォントを補助的に利用できる。
- 数式は最小16pxを確保し、縮小時も判読可能にする。

---

## 5. 形状・余白・影

### 5.1 Radius

| Token | Value | 用途 |
|---|---:|---|
| `--radius-xs` | 8px | 小型チップ、アイコンボタン |
| `--radius-sm` | 12px | 入力欄、通常ボタン |
| `--radius-md` | 16px | カード、プレビュー |
| `--radius-lg` | 22px | メインパネル |
| `--radius-pill` | 999px | タブ、ステータス |

### 5.2 Spacing

4pxを基準単位とし、主に `4 / 8 / 12 / 16 / 24 / 32px` を使用する。

- パネル外周余白：16px
- パネル間隔：12〜16px
- パネル内部余白：16px
- タイトルバー高：48px
- 標準ボタン高：38px
- 小型ボタン高：32px
- タッチ可能領域：最小44×44px

### 5.3 Shadow

```css
:root {
  --shadow-raised:
    8px 8px 18px rgba(24, 32, 48, 0.11),
    -8px -8px 18px rgba(255, 255, 255, 0.88);

  --shadow-raised-sm:
    4px 4px 10px rgba(24, 32, 48, 0.10),
    -4px -4px 10px rgba(255, 255, 255, 0.82);

  --shadow-inset:
    inset 3px 3px 8px rgba(24, 32, 48, 0.11),
    inset -3px -3px 8px rgba(255, 255, 255, 0.86);

  --shadow-focus:
    0 0 0 3px rgba(18, 103, 232, 0.24);

  --shadow-danger:
    0 0 0 3px rgba(217, 67, 78, 0.20);
}
```

- パネルは `--shadow-raised` を使用する。
- ボタン、カードは `--shadow-raised-sm` を使用する。
- 押下中、選択中、入力欄は `--shadow-inset` を使用する。
- 影の濃度は背景との差が強くなりすぎないよう抑える。

---

## 6. ワークスペースレイアウト

### 6.1 基本構成

1. グローバルヘッダー
2. ワークスペースキャンバス
3. 資料
4. ホワイトボード
5. AIチャット
6. スケッチ
7. 最小化パネル置き場

初期レイアウトは以下とする。

| Panel | 初期位置 | 推奨幅 | 最小サイズ |
|---|---|---:|---:|
| 資料 | 左 | 360px | 300×420px |
| ホワイトボード | 中央 | 可変・最大 | 520×420px |
| AIチャット | 右上 | 440px | 340×280px |
| 図表エリア | 右下 | 440px | 360×300px |

- ドッキング表示で通常パネルが2枚だけの場合、768px以上では `repeat(2, minmax(0, 1fr))` で左右均等に配置する。
- 767px以下では2パネルも1列で縦積みにし、どの表示数でも横スクロールを発生させない。

### 6.2 パネル操作

各パネルは独立したフローティングウィンドウとして扱う。

- タイトルバーのドラッグで移動。
- 自由配置時は右下の32px操作領域を持つハンドルでリサイズする。
- フォーカスされたパネルを最前面へ移動。
- 画面端や他パネルに近づくとスナップガイドを表示。
- レイアウト、サイズ、開閉状態をノート単位で保存。

タイトルバー右側の操作順序：

1. 最小化
2. 最大化 / 元に戻す
3. 閉じる

#### 最小化

- ワークスペース下部の「パネル置き場」にタイトル付きチップとして格納する。
- クリックで元の位置・サイズに復元する。

#### 最大化

- 選択したパネルをブラウザの表示領域いっぱいに最大化する。
- 最大化中はアプリ共通ヘッダー、ノート詳細ヘッダー、最小化パネル置き場を非表示にする。
- 最大化中はドッキング / 自由配置のワークスペース表示モードを非表示にし、パネル本体の表示領域を優先する。
- パネル自身のタイトルバーと「元に戻す」操作は常に表示する。
- パネル本文はタイトルバーを除く残りの高さを使用し、編集面・プレビュー・メッセージ一覧を画面下端まで広げる。
- Canvasは表示領域の変更を `ResizeObserver` で検知し、最大化と復元のたびに描画サイズを再計算する。
- `Esc` または同じボタンで復元する。

#### 閉じる

- 通常時はニュートラルなアイコン。
- Hover時のみ赤くする。
- 未保存の変更がある場合だけ確認ダイアログを表示する。
- パネルの内容は削除せず、表示状態だけをOFFにする。
- 閉じたパネルはヘッダーから再表示できる。

### 6.3 レイアウトプリセット

- 学習モード：資料 + ホワイトボードを大きく表示。
- 質問モード：AIチャット + 資料を大きく表示。
- 可視化モード：ホワイトボード + 図表エリアを大きく表示。
- 集中モード：選択パネルのみ最大化。

---

## 7. コンポーネント仕様

### 7.1 グローバルヘッダー

- 高さ：64px
- 左：ロゴ、アプリ名
- 中央：資料 / ホワイトボード / AIチャット / スケッチの表示切替
- 右：元に戻す、やり直す、自動保存状態、共有、アカウント

- 表示ONのパネルボタンは青文字と内側に沈む影で示す。
- 表示OFFのパネルボタンはモノクロのRaised表現にする。
- `ドッキング / 自由配置のアイコンボタン`はヘッダー内に置き、選択状態を`aria-pressed`で通知する。
- 各ボタンは表示状態を `aria-pressed` で通知する。
- パネルの閉じる操作はデータ削除ではなく表示OFFとし、対応するボタンから内容を保ったまま復元する。

### 7.2 Panel

```text
┌ Drag handle ─ Panel title ─────────── Min / Max / Close ┐
│ Panel toolbar                                                │
│                                                              │
│ Content                                                      │
│                                                              │
└──────────────────────────────────────────── Resize handle ───┘
```

- 背景：`--color-surface`
- Radius：22px
- 境界：1px solid rgba(255,255,255,.65)
- 通常：Raised
- フォーカス時：タイトルとドラッグハンドルを濃くし、薄い青の外周リングを付ける。

### 7.3 Button

#### Primary

- 青背景、白文字。
- Hoverで濃い青。
- Activeで内側に沈む影。
- 例：PDFをアップロード、送信、実行、共有。

#### Secondary

- モノクロのRaisedボタン。
- Hoverで文字またはアイコンを青にする。
- 例：ファイルを選択、ダウンロード、ページ追加。

#### Danger

- 通常は白地 + 赤文字。
- Hoverで薄赤背景。
- 確定操作のみ赤背景 + 白文字を使用。

#### Icon Button

- 32〜36px四方。
- Tooltipを必須とする。
- 選択時はInset + 青アイコン。

### 7.4 Input / Select

- 背景：`--color-surface-inset`
- Shadow：Inset
- Border：1px solid transparent
- Focus：青い3pxリング
- Error：赤枠 + 赤い補足文
- Placeholderだけで用途を説明せず、可能な限りラベルを付ける。

### 7.5 Tabs

- 非選択：モノクロ、Raisedなし。
- Hover：薄いRaised。
- 選択：Inset + 青文字 + 下線。
- 図表 / コードなど、表示モード切替に使用する。

### 7.6 Tooltip

- 背景：`#20242B`
- 文字：白
- 8px Radius
- 150〜250ms後に表示。
- アイコンだけの操作には必須。

### 7.7 Dialog

- 背景を黒の20%オーバーレイにする。
- ダイアログ本体は強いRaised表現。
- 通常確認はモノクロ + 青の確定ボタン。
- 削除確認は赤の確定ボタン。

---

## 8. 画面別仕様

### 8.1 資料

- パネル上部で `資料一覧 / 目次 / プレビュー` の3モードを切り替える。
- 選択したモードだけを表示し、狭いパネル内で一覧・目次・プレビューを同時表示しない。
- 資料、選択ページ、目次はノート単位で保持し、別ノートの内容を混在させない。

#### ファイル追加

- 大型ドロップゾーンは置かない。
- `PDFをアップロード` をPrimary、`ファイルを選択` をSecondaryとして横並びにする。
- ドラッグ＆ドロップは補助操作としてパネル全体で受け付けてもよいが、UI上の主導線はボタンとする。

#### 資料一覧

- PDFアイコン、ファイル名、容量、更新日、メニューを表示。
- 選択行はInset + 左端3pxの青ライン。
- 削除メニューは赤で表示する。

#### 選択中ページプレビュー

- 選択したサムネイルのページを、一覧直下に大きく表示する。
- プレビュー比率は原稿の縦横比を維持する。
- 通常表示ではサムネイル一覧を折りたたみ可能にし、折りたたみ中は選択ページの表示領域を縦に広げる。
- 最大化時はサムネイルを左約1/8の縦レール、選択ページを右側へ配置する。
- 最大化したCanvasプレビューは100%〜400%の範囲でピンチとボタンによるズームを提供する。
- Hoverで「拡大」「ホワイトボードへ貼る」を表示する。
- 選択中ページとサムネイルは同じ青い選択状態で同期する。

#### ページサムネイル

- 横スクロールまたは複数列グリッド。
- 選択中は青枠 + Inset。
- 検索結果やAI参照中のページには小さな青いAIマーカーを付ける。

### 8.2 ホワイトボード

- キャンバス本体は純白に近い `--color-surface-strong`。
- 外周だけニューモーフィズムとし、筆記面には強い影を入れない。
- ツールバーは浮遊するRaisedバーとして上部に固定する。
- Markdownに埋め込まれた手書き画像は選択すると手書き画像ダイアログを開く。
- 手書き画像ダイアログには `編集 / 削除` を配置し、削除時はMarkdown参照と画像データを同時に除去する。

#### ツール

- ペン、テキスト、図形、数式、消しゴム、選択。
- 選択中ツールはInset + 青。
- 太さ、色、元に戻す、やり直すは右側へ配置。

#### AI数式整形

- 手書き数式の近くに、整形候補をRaisedカードで表示。
- 採用は青、却下はモノクロ、誤変換報告は赤ではなく補助メニュー内に置く。
- AI生成・変換済み要素には小さな青い印を付ける。

#### グラフ

- 軸：`--color-text-primary`
- 補助線：`--color-border`
- 主系列：青
- 比較系列：グレー
- エラー・禁止領域・負の警告：赤

### 8.3 AIチャット

- パネル上部に新規チャットとチャット履歴のアイコン操作を固定表示する。
- 履歴はユーザー・ノート・スレッド単位で分離し、更新日時の新しい順で選択できる。
- 1スレッドは30ターンを上限とし、30ターン到達後は閲覧専用にする。
- 完了スレッドはAIで要約し、その要約を文脈に持つ新しいスレッドへ引き継げる。
- ユーザー発言：薄い青背景。
- AI発言：白またはライトグレーのRaisedカード。
- 数式ブロックは白背景 + 薄い境界線で独立させる。
- クイックアクションは小型のSecondaryボタン。
- 送信ボタンは青い円形Primary。
- 生成中は青いドットアニメーション。
- エラー時は薄赤カード + 再試行ボタン。

### 8.4 スケッチ

- 初期プロジェクトは `index.html`、`style.css`、`sketch.js` で構成する。
- JavaScriptファイルを追加でき、`index.html` のscriptタグ順で実行する。
- p5.jsのバージョンをパネル上部に表示する。
- 実行は親画面から分離した `sandbox="allow-scripts"` のiframe内だけで行う。
- 未許可の外部scriptは実行前に拒否し、runtime errorはスケッチ内へ表示する。

#### 図表タブ

- グラフ背景は白。
- グラフ自体にニューモーフィックな影は付けない。
- 拡大、ダウンロード、ホワイトボードへ配置を上部に置く。

#### コードタブ

- コードエディタはわずかに暗いInset面にする。
- 実行ボタンは青。
- 実行エラー箇所は赤い下線と行マーカーで表示。
- 成功表示には緑を使わず、青いチェックと「実行完了」を表示する。

---

## 9. インタラクションとモーション

| 対象 | Duration | Easing |
|---|---:|---|
| Hover | 120ms | ease-out |
| Pressed | 80ms | ease-in |
| パネル移動・スナップ | 160ms | cubic-bezier(.2,.8,.2,1) |
| 最小化・復元 | 200ms | cubic-bezier(.2,.8,.2,1) |
| 最大化 | 220ms | cubic-bezier(.2,.8,.2,1) |
| Dialog | 160ms | ease-out |

- Hover時の移動量は最大1px。
- パネルを大きく浮かせるアニメーションは使わない。
- `prefers-reduced-motion` では移動・拡大アニメーションを無効化する。

---

## 10. アクセシビリティ

- キーボードのみでパネル移動、リサイズ、最小化、最大化を操作可能にする。
- `Tab` のフォーカス順は、ヘッダー → パネルタイトルバー → パネル内操作の順とする。
- フォーカスリングを影だけで表現せず、青い輪郭を必ず表示する。
- 赤・青だけで状態を伝えず、アイコン、文言、形状を併用する。
- アイコンボタンには `aria-label` とTooltipを付与する。
- パネルのタイトルは見出し構造として認識可能にする。
- 数式には可能な限りMathMLまたは読み上げ用テキストを付与する。
- 文字倍率200%でも主要操作が欠落しないこと。

### キーボード例

| Shortcut | Action |
|---|---|
| `Ctrl / Cmd + S` | 保存 |
| `Ctrl / Cmd + Z` | 元に戻す |
| `Ctrl / Cmd + Shift + Z` | やり直す |
| `Ctrl / Cmd + Enter` | AI送信 / コード実行 |
| `Alt + M` | 選択パネルを最小化 |
| `Alt + Enter` | 選択パネルを最大化 / 復元 |
| `Esc` | 最大化解除 / Dialogを閉じる |

---

## 11. レスポンシブ方針

### Desktop: 1280px以上

- フローティングパネルを全面的に利用する。
- 初期状態で3カラム構成。

### Tablet: 768〜1279px

- パネルはスナップ式の2カラムを基本とする。
- 同時表示は最大3パネル。
- AIチャットと図表エリアはタブ切替でもよい。

### Mobile: 767px以下

- フローティング移動・自由リサイズは無効化する。
- 1画面1パネルを基本にし、下部ナビゲーションで切り替える。
- 最小化ではなく、パネル履歴スタックとして扱う。
- ホワイトボードは横方向のパンとピンチズームに対応する。

---

## 12. CSS Design Tokens

```css
:root {
  color-scheme: light;

  --color-bg: #eef1f5;
  --color-surface: #f4f6f9;
  --color-surface-strong: #ffffff;
  --color-surface-inset: #e9edf2;
  --color-border: #d6dce5;

  --color-text-primary: #171a20;
  --color-text-secondary: #5f6875;
  --color-text-muted: #8c95a3;
  --color-icon: #343a44;

  --color-blue: #1267e8;
  --color-blue-hover: #0d56c5;
  --color-blue-active: #09459f;
  --color-blue-soft: #e7f0ff;
  --color-blue-focus: #6ea4ff;

  --color-red: #d9434e;
  --color-red-hover: #bb313b;
  --color-red-active: #96262e;
  --color-red-soft: #fceaec;

  --radius-xs: 8px;
  --radius-sm: 12px;
  --radius-md: 16px;
  --radius-lg: 22px;
  --radius-pill: 999px;

  --space-1: 4px;
  --space-2: 8px;
  --space-3: 12px;
  --space-4: 16px;
  --space-6: 24px;
  --space-8: 32px;

  --shadow-raised:
    8px 8px 18px rgba(24, 32, 48, 0.11),
    -8px -8px 18px rgba(255, 255, 255, 0.88);

  --shadow-raised-sm:
    4px 4px 10px rgba(24, 32, 48, 0.10),
    -4px -4px 10px rgba(255, 255, 255, 0.82);

  --shadow-inset:
    inset 3px 3px 8px rgba(24, 32, 48, 0.11),
    inset -3px -3px 8px rgba(255, 255, 255, 0.86);
}
```

### 基本パネル例

```css
.workspace-panel {
  background: var(--color-surface);
  border: 1px solid rgba(255, 255, 255, 0.68);
  border-radius: var(--radius-lg);
  box-shadow: var(--shadow-raised);
  color: var(--color-text-primary);
  overflow: hidden;
}

.workspace-panel:focus-within {
  outline: 2px solid var(--color-blue-focus);
  outline-offset: 2px;
}
```

### ボタン例

```css
.button-secondary {
  min-height: 38px;
  padding: 0 16px;
  color: var(--color-text-primary);
  background: var(--color-surface);
  border: 1px solid rgba(255, 255, 255, 0.72);
  border-radius: var(--radius-sm);
  box-shadow: var(--shadow-raised-sm);
}

.button-secondary:active,
.button-secondary[aria-pressed="true"] {
  color: var(--color-blue);
  box-shadow: var(--shadow-inset);
}
```

---

## 13. Do / Don't

### Do

- 白・グレーの階層を、面・境界・影で丁寧に分ける。
- 青は「いま操作できるもの」「選択中のもの」に使う。
- 赤は破壊的操作とエラーだけに使う。
- ホワイトボード、PDF、図表などのコンテンツ面はフラットに保つ。
- 影に加え、アイコン、テキスト、フォーカス枠で状態を説明する。

### Don't

- すべての要素に強い影を付けない。
- 青と赤を装飾目的で大量に使わない。
- 灰色文字を多用して可読性を落とさない。
- RaisedとInsetの意味を画面ごとに変えない。
- 移動可能なパネルと固定領域を、見た目だけで曖昧にしない。

---

## 14. 完成時の印象

- 数学・研究用途に耐える落ち着いた作業画面。
- モノクロの静けさの中で、青い操作箇所が自然に目に入る。
- 赤は必要な場面でのみ強く認識される。
- ニューモーフィズムによる柔らかさがありながら、業務ツールとして明確に操作できる。
- ユーザーが資料、ノート、AI、図表を自分の机の上のように自由に配置できる。
